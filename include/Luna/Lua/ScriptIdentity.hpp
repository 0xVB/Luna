#pragma once

namespace Luna {
    enum ScriptIdentity {
        core,   // high api permission level
        mod,    // medium api permission level
        util    // low api permission level
    };
}