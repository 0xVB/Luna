#pragma once

namespace Luna {
    enum ScriptIdentity {
        CORE_ID,   // high api permission level
        MOD_ID,    // medium api permission level
        UTIL_ID    // low api permission level
    };
}